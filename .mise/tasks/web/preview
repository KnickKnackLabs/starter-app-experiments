#!/usr/bin/env bash
#MISE description="Build and preview the web app locally (production mode)"
#MISE depends=["web:build"]

set -eo pipefail

echo "Previewing production build..."
pnpm --filter web-app run start
